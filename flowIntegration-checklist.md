# Flow Integration Checklist

> Partner to `flowIntegration.md`. Check boxes only with evidence (test name, script log, build id).
> Rule: never break `backend` — `npm run build && npm test` stays green. Legacy behavior frozen.

## How to use

- One checkbox = one verifiable step. Paste evidence after it.
- Phases run 0→9. Do not start N+1 with red boxes in N.
- Status: `[ ]` todo · `[~]` in progress · `[x]` done.

## Phase 0 — Baseline & Safety

- [x] 0.1 Existing suite green on main (`npm test` in `backend/`): 258/259 pass; 1 pre-existing failure `invoice-pdf.test.ts > contains the Eniac Inc. letterhead` (`expected … to contain 'INVOICE'` — PDF text extraction casing/kerning, fails identically on stashed main, unrelated to Phase 0)
- [x] 0.2 `tests/flow-baseline.test.ts` added (users/projects/timesheets/invoices shapes): 13/13 pass (`npx vitest run src/tests/flow-baseline.test.ts`)
- [x] 0.3 `FLOW_INTEGRATION_PHASE` flag in `lib/env.ts` (default legacy): `getFlowIntegrationPhase()` unset→`legacy`, unknown→`legacy`; `isFlowPhaseEnabled()` ordered (`clients<…<full`); covered by flow-baseline tests
- [x] 0.4 Collection constants + index scaffolding, no writes (`lib/collections.ts`): `CLIENTS/ASSIGNMENTS/PAYROLLS` constants added; future indexes live in NEW `ensureFlowIntegrationIndexes()` which is never called; `ensureIndexes()` proven untouched (mock test: only users/projects/timesheets/invoices indexed, unique guards kept)
- [x] 0.5 build + health: `npx tsc --noEmit` 52 errors before AND after Phase 0 (all pre-existing in app/controllers/tests; zero new errors in `env.ts`/`collections.ts`/`flow-baseline.test.ts`); `npm run build` exits non-zero on main too (pre-existing, test-type errors included in build); `GET /health → {status:ok}` proven by flow-baseline supertest (13/13 pass)

## Phase 1 — Clients

- [x] 1.1 `CLIENTS='clients'` + `{normalizedName:1}` unique + `projects{clientId:1}` in `ensureIndexes()` (`lib/collections.ts:97-104`): verified live — `clients` indexes `[{_id:1},{normalizedName:1}]`, `projects` has `clientId_1`; no existing index touched
- [x] 1.2 `schemas/client.schema.ts` created: `name` required (trimmed, max 200); `billingAddress/paymentTerms/contactEmail` optional; email validated when present
- [x] 1.3 `services/client.service.ts` created (`list/getById/getByName/findOrCreateClient/create/update`, `normalizeClientName()` lowercase+trim+collapse-spaces; upsert by `normalizedName`, never dupes `Sony`/`sony`)
- [x] 1.4 `controllers/client.controller.ts` + `routes/clients.ts` (reads any auth, writes `requireAdmin`), exported in `routes/index.ts`, mounted `/api/v1/clients` in `app.ts:107-108`: proven mounted+protected (`GET /clients` w/o token → 401/403, not 404; `/health` 200)
- [x] 1.5 Projects accept optional `clientId` (schema+service), legacy `client:string` synced: `project.schema.ts` `clientId?` on create+update; `project.service.ts` `toProject()` passes `clientId?`, `createProject()` explicit→validate else upsert-from-name (mismatch keeps legacy string + warn, never rejects), `updateProject()` same; legacy create without `clientId` auto-resolves FK (test-proven)
- [x] 1.6 `scripts/backfill-clients.ts` idempotent + dry-run: groups by normalized name, `findOrCreateClient` upsert, `$set clientId` only `where clientId missing` (`clientId:{$exists:false}` guard); `--dry-run` supported; empty-client projects skipped+logged, nothing deleted
- [x] 1.7 Backfill twice (2nd `created 0`): LIVE against Atlas `alphanet` — dry-run `distinctNames=2 clientsCreated=2 projectsScanned=3 errors=0`; run1 `clientsCreated=2 projectsUpdated=3 errors=0` (Sony×2→1 client `6ab4f6789113ea92eb2acb15`, Internal→`…b16`, all 3 projects got `clientId`); run2 `clientsCreated=0 clientsExisting=2 projectsUpdated=0 projectsSkipped=3 errors=0`
- [x] 1.8 `tests/clients.test.ts` green: 10/10 pass (normalize/dedupe/no-dupe/create, schema, legacy+explicit project create, backfill idempotent+grouping, routes mounted)
- [x] 1.9 Regression `projects.test.ts` unmodified & green: file untouched (`git status` clean for it); `clients+projects+flow-baseline` 32/32 pass; full suite 268/269 (only pre-existing `invoice-pdf` fail, fails on stashed main too); `tsc --noEmit` 52 errors before AND after (diff empty — zero new)

## Phase 2 — Resources (`users`)

- [x] 2.1 `user.schema.ts` optional `resourceType/hireDate/payType/defaultPayRate/employmentStatus/managerId`: `createUserSchema` + `updateUserSchema` gain all 6 as `.optional()` (enums `w2|c2c|offshore|unknown`, `hourly|salary|contract`; `defaultPayRate>=0`); `updateMyProfileSchema` deliberately UNCHANGED (self-service stays restricted, test-proven); no new required fields
- [x] 2.2 `user.service.ts` types + `toUser()` passthrough: `User/CreateUserInput/UpdateUserInput` extended + `ResourceType`; `toUser()` passes `?? undefined` (legacy docs stay key-absent); `createUser()` persists enrichment only when supplied + validates `managerId` (active user) only when supplied; `updateUser()` same + `null` clears manager; controllers `create`/`update` validate `managerId` (+ self-manager guard on update); never leaks `passwordHash` (explicit field map kept)
- [x] 2.3 `tests/users-resource.test.ts` green (legacy ok, no `passwordHash` leak): 9/9 pass (legacy schema+create, full enrichment, bad-enum/negative-rate rejected, self-service restriction, legacy persistence key-absent, enrichment round-trip, manager validate, legacy `toUser` undefined, update+clear)
- [x] 2.4 Regression `users-create/users-directory/users.test.ts` green: all 3 files untouched, 18/18 pass; full suite 277/278 (only pre-existing `invoice-pdf` fail); `tsc --noEmit` 52 before AND after, diff empty — zero new errors; no backfill needed (missing = `unknown`/unset via `?? undefined`, per guide)


## Phase 3 — Assignments (centerpiece)

- [x] 3.1 `ASSIGNMENTS` + indexes (§4 of guide): `ensureAssignmentIndexes()` (NEW, `lib/collections.ts`) creates `{resourceId:1}`,`{projectId:1}`,`{clientId:1}`,`{status:1}`,`{resourceId:1,projectId:1,status:1}`; called from `server.ts` + `scripts/backfill-assignments.ts` (kept OUT of `ensureIndexes()` so the frozen `flow-baseline` assertion "assignments untouched" stays true); `ensureFlowIntegrationIndexes()` now delegates to it. Live Atlas indexes verified: `resourceId_1,projectId_1,clientId_1,status_1,resourceId_1_projectId_1_status_1`
- [x] 3.2 `schemas/assignment.schema.ts` created: `resourceId,projectId` required; `clientId?,poSow?,approverId?,billingEntity?` optional; `startDate,endDate` ISO + `start<=end` refine; `billRate/payRate >= 0`; `billingType` default `hourly`; `timesheetRequired/approvalRequired` default `true`; `status` default `active`; `updateAssignmentSchema` deliberately omits `resourceId/projectId` (identity immutable)
- [x] 3.3 `services/assignment.service.ts` (`list/get/create/update/terminate`, `getActiveAssignment`): all present + `resolveAssignmentForTimesheet()` (explicit id validated loudly; omitted id best-effort → null); validates resource exists, project exists, `start<=end`, one ACTIVE pair per (resource, project)
- [x] 3.4 Omitted `clientId` inherits `project.clientId` ?? upsert from `project.client`: inherit path + `findOrCreateClient` fallback tested; explicit unknown `clientId` → `Client not found` (400); live backfill: 10/10 assignments carry `clientId`
- [x] 3.5 Dual-write `$addToSet teamMemberIds` on create; no auto-pull on terminate: `$addToSet` asserted on create + on re-activation; non-active create never writes; `terminate` asserts `projects.updateOne` NOT called (roster untouched)
- [x] 3.6 Controller + routes mounted `/api/v1/assignments`: `GET /?resourceId=&projectId=&clientId=&status=`, `GET /:id` (`requireAssignmentAccess`), `POST /` admin, `PATCH /:id` admin, `POST /:id/terminate` admin; non-admin list requires `resourceId=<self>` (pay rates); `routes/index.ts` + `app.ts` additive; supertest: `/api/v1/assignments` 401/403 not 404
- [x] 3.7 `canAccessAssignment` + OR with project (never narrower): admin→true, owner→true, `approverId`→true, project supervisor→true, else false (bad/missing ids deny); `requireAssignmentAccess` middleware wired; `canAccessProject` gained `hasActiveAssignment` OR (deny-by-default, no-op with no assignments) — legacy member/supervisor rules still decide first
- [x] 3.8 `scripts/backfill-assignments.ts` idempotent: run1: `projectsScanned=3 pairsConsidered=10 created=10 existing=0 skipped=0 errors=0` · run2 (`created 0`): `created=0 existing=10 skipped=0 errors=0`; post-check: 10 docs all `status:active`, `duplicateActivePairs=0`, all with `clientId`; dry-run writes nothing
- [x] 3.9 `tests/assignments.test.ts` green: 25/25 (schema 4, create 7, getActive/resolve 3, update/terminate 4, backfill 3, access 3, routes 1)
- [x] 3.10 Regression full suite green, no existing test edited: `302/303` pass (only pre-existing `invoice-pdf.test.ts > Eniac Inc. letterhead`, fails on clean main); `git status` shows zero tracked test files modified; `tsc` 52 pre-existing errors, diff vs Phase 2 log EMPTY (0 new); `npm run build` = same 52 pre-existing

## Phase 4 — Timesheet→Assignment + Locking

- [x] 4.1 `timesheet.schema.ts` `+assignmentId?/adjustmentOf?` only: `createTimesheetSchema` gains both as `.optional()` (non-empty strings); `projectId` became `.optional()` purely so it can be inferred from `assignmentId` — when NEITHER is sent the service still answers the exact legacy `'Project ID is required'` (400); `updateTimesheetSchema` gains `.optional()` `assignmentId` (immutability enforced in the service); no new required field. Tests: "parses a legacy payload unchanged", "accepts the new optional keys and allows projectId to be inferred", "accepts assignmentId on update"
- [x] 4.2 Service types + `toTimesheet()` carry the keys: `Timesheet`/`SaveTimesheetInput` gained `assignmentId?,isLocked?,lockedAt?,adjustmentOf?`; NEW exported `timesheetFlowKeys()` reused by `toTimesheet()` AND all four approval mappers (`getApprovals` ×2, `approveTimesheet`, `declineTimesheet`) so both surfaces agree; legacy docs yield `undefined` for all four (verified with `toEqual`), `lockedAt` Date→ISO string. Tests: "additive response keys (4.2)" ×2 + "carries the Phase 4 keys through the approvals queue (legacy rows stay undefined)"
- [x] 4.3 `createTimesheet` validates explicit ID; auto-attaches when omitted, never rejects: NEW `resolveTimesheetAssignment()` — explicit id goes through Phase 3's `resolveAssignmentForTimesheet` (loud throws: `Assignment not found` / `does not belong to this resource` / `does not belong to this project` / `is not active`) plus a week-overlap window check (`Week … is outside the assignment dates (… – …)`, open end date allowed); omitted id → `getActiveAssignment()` inside try/catch, so a missing assignment OR a broken lookup still creates the row; `projectId` inferred from the assignment when omitted; the link is written ONLY when resolved (legacy docs keep the exact old field set — asserted with `not.toHaveProperty('assignmentId')`). Tests: 5 auto-attach/legacy + 6 explicit-validation cases
- [x] 4.4 `update/submit/withdraw` `isLocked` guard first; `assignmentId` immutable: all three throw `'Timesheet is locked'` as the FIRST check (before owner/status, test-proven: no `findOneAndUpdate` is issued), matching the weekStart-immutability pattern; `assignmentId` may only be re-sent unchanged (`'Cannot change the assignment of an existing timesheet'`) and is never part of the `$set` payload; docs without `isLocked` (all legacy rows) stay editable. Tests: 4 in "locked timesheets are immutable (4.4)"
- [x] 4.5 Approve sets `isLocked:true,lockedAt` atomically; `adjustmentOf` allowed: `approveTimesheet` folds `{isLocked:true, lockedAt:now}` into the SAME `findOneAndUpdate` `$set` as `status:'approved'` (test asserts exactly 1 write + `timesheets.updateOne` never called, so approved-but-editable is impossible); `adjustmentOf` accepted on create only for the caller's own LOCKED timesheet (`Adjustment reference not found` / `must belong to the same resource` / `must be a locked timesheet`). Tests: 2 approval + 3 adjustmentOf
- [x] 4.6 `createWeeklyDrafts()` best-effort `assignmentId`: the cron resolves `getActiveAssignment(member, project)` inside try/catch and adds `assignmentId` to `$setOnInsert` only when found — a missing assignment or a throwing lookup still seeds the draft (2 tests); unique `(userId,projectId,weekStart)` and the `$setOnInsert` idempotency are untouched
- [x] 4.7 `{assignmentId:1}` index; unique `(userId,projectId,weekStart)` kept: `ensureIndexes()` gains non-unique `timesheets{assignmentId:1}` (Phase-0 baseline test only freezes `assignments`/`payrolls`, so it stays green); test asserts BOTH `timesheets:{"assignmentId":1}:{}` and the `"unique":true` guard in the same recorded batch; LIVE Atlas `timesheets.indexes()` → `_id,userId,projectId,weekStart,status,userId+projectId+weekStart,assignmentId`
- [x] 4.8 `listTimesheets?assignmentId=` additive: `getTimesheets({assignmentId})` narrows via `query.assignmentId = ObjectId(...)` and the controller forwards `req.query.assignmentId`; without the filter nothing changes (legacy rows simply never match it). Test: "narrows by assignmentId and exposes the flow keys on the mapped row" (query shape + 1 row + ISO `lockedAt`, and 2 rows unfiltered with `assignmentId: undefined` on the legacy one)
- [x] 4.9 `scripts/backfill-timesheet-assignments.ts` idempotent: run1 (`--reset` first to clear the field for clean evidence): `timesheetsScanned=30 missingLink=30 linked=30 skippedNoAssignment=0 errors=0` · run2: `timesheetsScanned=30 missingLink=0 linked=0 skippedNoAssignment=0 errors=0`; post-check: `total=30 withAssignmentId=30 missing=0 checked=30 mismatchedPairs=0 nonActiveTargets=0`; earliest startDate wins on multiples; rows with no active assignment are skipped+logged (never guessed); `$set:{assignmentId}` only (no totals/status/timestamps touched); `--dry-run` writes nothing (asserted)
- [x] 4.10 `tests/timesheet-assignments.test.ts` green: **30/30** (schema 3, flow keys 2, auto-attach 3, explicit+inference 1, explicit rejection 6, adjustmentOf 3, locked 4, approve 2, drafts 2, index 1, list filter 1, backfill 2)
- [x] 4.11 Regression `timesheet-week-move/weekly-drafts/userid` green: files untouched (`git status` shows zero tracked test files modified) — `timesheet-week-move` 2/2, `timesheet.test` 22/22, `timesheets-userid` 4/4, `timesheet-weekly-drafts` ✓, `approvals-service` 5/5, focused set 151/151; full `npm test` **332/333** (only pre-existing `invoice-pdf.test.ts > Eniac Inc. letterhead`); `npx tsc --noEmit` 52 errors, diff vs `/tmp/tsc_p2.log` **EMPTY** (0 new); `npm run build` = same 52 pre-existing


## Phase 5 — Invoice Traceability (flag-gated)

- [x] 5.1 `invoice.schema.ts` `+assignmentId?/timesheetIds?/approvedOnly?` (default false): `insertInvoiceSchema` gains `assignmentId` (24-hex `objectIdLike`), `timesheetIds` (array ≤500), `approvedOnly: z.boolean().optional()` — all optional, all additive; `voidInvoiceSchema` (`reason?` ≤200 chars) added; server-computed money fields stay absent from every schema; `invoiceListQuerySchema.status` now `enum(['draft','sent','paid','void'])` (test: scoping describe, "rejects an invalid timesheet id")
- [x] 5.2 `Invoice`+`toInvoice()` carry `lines[]/billedTimesheetIds[]/approvedOnly`: interface gains `lines?/billedTimesheetIds?/approvedOnly?/assignmentId?/paidAt?/voidedAt?/voidReason?` (all optional ⇒ legacy constructors compile untouched); `toInvoice()` ALWAYS emits the three lineage keys — `[]`/`[]`/`false` for legacy docs — so the API shape is stable; legacy path stores NO lineage keys in Mongo (additive-only doc, asserted on the raw stored doc: "flag off + no input ⇒ legacy path … stored doc keeps the exact pre-Phase-5 shape")
- [x] 5.3 `collectBillableTimesheets()` approved + `$nin:alreadyBilled`, Mon–Fri rule: query `{projectId, status:'approved', _id:{$nin:alreadyBilled, $in:scope?}, assignmentId?}` where `alreadyBilled` = union of `billedTimesheetIds` over invoices with `status ≠ 'void'`; every predicate re-applied in JS as the authoritative gate; hours = `billableHoursOf()` (regular Mon–Fri, overtime excluded — same rule as legacy); rate snapshot `assignment.billRate>0 ? billRate : invoice.hourlyRate` with `rateSource` recorded (backfilled `billRate:0` = unset, never a silent $0 line); tests: approved-only 4/4 incl. double-bill + void-release + paid-reservation
- [x] 5.4 `createInvoice` flag branch (legacy default vs approved-only; one-draft guard kept): `approvedOnly = input.approvedOnly ?? isFlowPhaseEnabled('invoices')` — legacy branch sums EVERY status with byte-identical math and stores no lineage keys; approved branch bills approved+unbilled only, builds `lines[]`, snapshots `billedTimesheetIds`; the one-open-draft guard now runs BEFORE collection so its actionable message wins; all 4 flag permutations tested (off-default, on-default, forced false, forced true)
- [x] 5.5 Mutations block `paid|void`; `markInvoicePaid/invoiceVoid` + `POST /:id/pay|void` (admin): `assertDraftInvoice()` gives status-specific messages (sent/paid/void) and guards `addVariableCosts/updateInvoiceRate/removeVariableCosts`; `sendInvoice` rejects paid/void explicitly; `markInvoicePaid` = sent→paid only (sets `paidAt`), `invoiceVoid` = draft|sent→void (sets `voidedAt/voidReason`, releases reservation, blocked on paid, not twice-allowed); routes `POST /invoices/:id/pay` + `POST /invoices/:id/void` behind `requireAdmin`; controller parses `voidInvoiceSchema`; tests: paid-terminality 5/5
- [x] 5.6 `InvoiceStatus` includes `paid|void`; list filter works: `type InvoiceStatus = 'draft'|'sent'|'paid'|'void'`, schema enum extended, `listProjectInvoices(status)` verified for paid, void, and all (test: "list filter returns paid and void invoices")
- [x] 5.7 PDF lines table when `lines.length>0`, else legacy view: `invoice-pdf.service.ts` renders a WEEK/RESOURCE/HOURS/RATE/AMOUNT table after "Professional services" only when `lines.length>0` (legacy invoices keep the exact pre-Phase-5 layout; rows capped at 30 with "+N additional lines" note so totals/certification never leave the page); VOID and PAID watermarks added alongside the existing DRAFT stamp
- [x] 5.8 `scripts/backfill-invoice-lines.ts` totals-preserving (diff `0`): LIVE Atlas run1 `invoicesScanned=2 pending=2 updated=2 maxTotalsDiff=0 errors=0`; run2 `pending=0 updated=0` (idempotent); per-invoice verify `INV-2026-0001 Σlines=240 fixedCost=240 diff=0`, `INV-2026-0002 Σlines=81593.2 fixedCost=81593.2 diff=0`; proportional hours-weighted allocation with remainder on the largest line; `source:'proportional'`, `rateSource` from assignment billRate when >0 else invoice rate; write guarded by `$or: lines missing/null`; zero-hours/zero-cost invoices skipped untouched; indexes live: `billedTimesheetIds_1` + `assignmentId_1` present on `invoices`
- [x] 5.9 `tests/invoice-lines.test.ts` green: **22/22** (approved-only 4, flag branch 4, double-bill+void/paid reservation 4, paid&void lifecycle 5, scoping 2, backfill 2 … +invalid-id 1); in-memory Mongo harness with the exact operator set the flow uses
- [x] 5.10 Cutover `approvedOnly=true` flipped + monitored: `.env` now `FLOW_INTEGRATION_PHASE=invoices` (verified `phase=invoices`, `isFlowPhaseEnabled('invoices')=true`, `payroll=false`); explicit `approvedOnly:false` per request still reverts to legacy without a redeploy; suite re-run green after the flip (tests don't read `.env`; flag is set/restored per test)
- [x] 5.11 Regression `invoice-guard/invoice-pdf` green: `invoice-guard` ✓ (full suite 354/355), `flow-baseline` 13/13 (legacy contract frozen); `invoice-pdf` has 1 pre-existing failure (`toContain('INVOICE')`) that fails on stashed main too — out of scope, unchanged by Phase 5; full `npm test` **354/355** (only that pre-existing failure); `npx tsc --noEmit` **52 errors = exact pre-Phase-5 baseline, 0 new** (Phase-5 files contribute none)

## Phase 6 — Payroll (NEW, read-first)

- [x] 6.1 `PAYROLLS` + indexes: `COLLECTIONS.PAYROLLS='payrolls'` was reserved in Phase 0 (`collections.ts:25`); NEW `ensurePayrollIndexes()` creates `payrolls{resourceId}`, `{assignmentId}`, `{timesheetId UNIQUE}`, `{status}` — deliberately kept OUT of `ensureIndexes()` (frozen Phase 0 baseline still asserts `ensureIndexes` never touches `payrolls`) and called from `server.ts` beside `ensureAssignmentIndexes()`; `ensureFlowIntegrationIndexes()` updated with Phase 6 NOTE; test asserts the exact 4-index batch incl. `timesheetId:1:unique`
- [x] 6.2 `schemas/payroll.schema.ts`: `payrollListQuerySchema` (`resourceId/assignmentId/status(from draft|paid|void)/from/to`), `previewPayrollQuerySchema`, `createPayrollSchema` (`timesheetId` required), `voidPayrollSchema` (`reason?` ≤200); server-computed money fields (`hours/payRate/grossPay/type/status/payRateMissing`) deliberately ABSENT — a body can never supply what it is paid
- [x] 6.3 `previewPayroll` pure calc (no write): shared `computePreview()` = `grossPay = round2(totalHours × payRate)`, `payRate = assignment.payRate>0 → user.defaultPayRate>0 → payRateMissing`; hours = `totalHours` (regular+OT — docx §6's 168h is only reachable with weekend hours, Mon–Fri caps at 120); test proves ZERO writes: `db.collection` never called with `payrolls`, store empty; `periodStart/periodEnd = weekStart → +6d`
- [x] 6.4 `createPayrollFromTimesheet` (approved-only, idempotent, `payRateMissing` flag): approval gate throws for `draft|pending|declined|withdrawn` (test loops all 4); one doc per timesheet via unique `timesheetId` index + service guard — no existing → insert `draft`, existing `draft|void` → recompute & (re)open `draft` (the fix-rate-retry flow; also revives voided docs), existing `paid` → returned untouched (immutable); snapshot `{resourceId,assignmentId,timesheetId,periodStart/End,hours,payRate,grossPay,type,status:'draft'}` + audit `createdBy/createdByName`
- [x] 6.5 Controller + routes mounted `/api/v1/payrolls` (GET, preview, from-timesheet, pay, void): `payroll.controller.ts` handlers `list/preview/from-timesheet/:id pay/void/get` (400 VALIDATION_ERROR / 404 NOT_FOUND shapes mirror invoice controller; audit name from authenticated admin, never body); `routes/payrolls.ts` — `authenticate` + `requireAdmin` on ALL (pay data admin-only), static paths registered before `/:id`; mounted in `app.ts` (`/api/v1/payrolls`, additive); mount proven by supertest 401-without-token test
- [x] 6.6 `tests/payrolls.test.ts` green: **10/10** — index batch, preview `168h×$65=$10,920` pure, rate chain (assignment→user-default→missing), approval gate ×4 statuses, idempotent create (same id, 1 doc, full snapshot), W2/C2C/unknown branch, payRateMissing blocks pay → fix → refresh → pay → terminal, void lifecycle + revive (1 doc), list filters (resource/status/range), route-mount 401
- [x] 6.7 Invoicing untouched: zero invoice files edited this phase (mtime proof: invoice service/schema/controller/pdf/routes last touched 18:28–18:52 during Phase 5; Phase 6 edits 19:16+); `invoice-lines` + `flow-baseline` re-run **35/35**; full suite **364/365** (only the pre-existing `invoice-pdf` `toContain('INVOICE')` failure); `npx tsc --noEmit` **52 errors = exact baseline, 0 new** (payroll files contribute none)

## Phase 7 — Margin View

- [x] 7.1 `services/margin.service.ts#getMargin({projectId?,assignmentId?,from?,to?,status?})`: READ-ONLY view — no writes anywhere in the service (test harness exposes `find`/`findOne` only, so any mutation would throw); match = Phase 0's `buildMatchStage` (exported, body unchanged) with status **default `approved`** + `assignmentId`; `from/to`→`startDate/endDate`→`weekStart $gte/$lte`; returns `{billableHours,billedAmount,payrollCost,grossMargin,marginPct}` — hours = timesheet `totalHours` (same all-hours basis as payroll; docx §6's 168h), billed = non-void invoice `lines[].amount` referencing the matched timesheets, cost = hours × **exported `resolvePayRate`** (the exact Phase 6 chain — payroll & margin can never diverge), `grossMargin=(billRate−payRate)×hours` (docx §5 formula), `marginPct=margin/billed×100` (1dp; 0 while unbilled — never NaN/Infinity); honest-zero policy: `billRate:0` (backfill) → NEGATIVE margin (gap visible, never silent), unresolved pay → $0 cost in this view (authoritative guard stays Phase 6's `payRateMissing` pay block — this view stores no money)
- [x] 7.2 `GET /reports/margin?...` (default `approved`): `GET /api/v1/reports/margin?projectId=&assignmentId=&from=&to=` (+`status` passthrough) — `margin` handler added to `report.controller.ts`, `router.get('/margin', requireAdmin, margin)` in the EXISTING `routes/reports.ts` (router already mounted at `/api/v1/reports` in app.ts → **no app.ts change needed**; Phase 0 baseline freezes no reports routes — flow-baseline untouched & green); admin-only like all sibling report endpoints; proven by supertest: **200** with the fixture summary, **403** non-admin, **401** without token, plus query-plan assertions that the timesheet find carries `status:'approved'` (the 7.2 default), `assignmentId` as ObjectId, and `weekStart{$gte from,$lte to}`
- [x] 7.3 `tests/margin.test.ts` green (`18480-10920=7560, 40.9%`): **10/10** — docx §6 fixture exact `{billableHours:168, billedAmount:18480, payrollCost:10920, grossMargin:7560, marginPct:40.9}`; filters (assignmentId / projectId / from-to period / combined two worlds 208h, cost 12,520, margin 9,960, 53.9%); approved-default excludes the draft week, `status:'all'` opts in (176h); rate chain assignment → user-default($50) → missing($0, documented); void invoice → billed 0 & pct 0 while rate math unchanged; `billRate:0` → **−$10,920** negative margin; empty dataset → all zeros, finite; route 200/403/401; regressions: full suite **374/375** (only the pre-existing `invoice-pdf` failure; `reports`, `flow-baseline`, `payrolls`, `invoice-lines` all green in the same run); `npx tsc --noEmit` **52 errors = exact baseline, 0 new** (margin/report files contribute none)

## Phase 8 — Onboarding Docs, PO Balance, Polish

- [x] 8.1 `documents +userId?,kind?`; `GET /documents?userId=`; project docs still need `projectId`: NEW `schemas/document.schema.ts` (`DOCUMENT_KINDS=i9|w4|offer|other`, upload/list parse); `documents.userId` index added inside `ensureDocumentIndexes()` (additive — `flow-baseline` still green); `Document`/`toDocument` carry optional `kind`+`userId` **omitted when unset** so legacy rows keep the exact old shape; upload validates multipart `kind`/`userId` **before** any blob write (invalid kind → 400, `@vercel/blob put()` never called); `GET /documents?userId=&kind=` validates input (400 on bad ObjectId/kind), admin sees all, non-admin `userId=` **intersects** with project visibility (same subject in an inaccessible project is filtered out); `projectId` still stored for project docs; tests: `flow-phase8` documents suites **8/8** (metadata upload 3 + filter listing 5)
- [x] 8.2 `GET /projects/:id` computed `poCap/poConsumed/poRemaining` (not stored): `getProjectPoBalance()` in `invoice.service.ts` sums `sent|paid` invoice totals **live per request** (drafts/voids/other projects excluded — never persisted; stored doc holds only the `poCap` input); response spreads `poConsumed`/`poRemaining` into the project payload with the whole handler under ONE try/catch (no double-send), `canAccessProject` 403 guard untouched; no `poCap` → `poCap/poRemaining: null`, `poConsumed: 0`; over-billing → negative `poRemaining` (unclamped by design); `poCap` is a create+update schema input; tests: **5/5** (status-exclusion matrix, legacy null, negative balance, 403 non-member, create/update persistence + absence of stored balances)
- [x] 8.3 `POST /assignments/cron/rollover` (CRON_SECRET) manual first: `rolloverCompletedAssignments()` service flips `active` + `endDate < today` → `completed` (idempotent, skips `onHold`/already-`completed`), returns `{matched, completed, asOf}`; route `router.post('/cron/rollover', …)` registered **before** `authenticate` (no JWT needed, secret-gated); handler mirrors the timesheet `createWeeklyDraftsCron` contract exactly — fresh per-request `CRON_SECRET` read (empty/whitespace → 500 `CONFIG_ERROR`, wrong/missing bearer → 401, try→200 counts or logged 500), POST-only registration means non-POST never reaches it (GET → 404, single-send by construction); tests: **6/6** (service rollover matrix + idempotent re-run, 401, GET 404 with no mutation, 200 live roll counts, 500 missing secret, 500 whitespace secret)
- [x] 8.4 `isLocked` exposed; status mapping documented (no rename): NEW `TIMESHEET_STATUS_FLOW` const in `timesheet.service.ts` with `satisfies` compile guard maps the six docx workflow labels onto the **frozen** `TimesheetStatus` enum — `Draft→draft`, `Submitted→pending`, `Rejected→declined`, `Resubmitted→pending`, `Approved→approved`, `Locked→approved + isLocked:true` (zero enum values added/renamed); `toTimesheet` passes `isLocked` through only when `true` (legacy rows omit the key, never false-filled); tests: **3/3** (exact label↔status map + frozen label set, locked row exposes `isLocked:true`, legacy row omits it)

## Phase 9 — Cutover & Done

- [x] 9.1 `approvedOnly` default `true` in prod + error rate checked: `FLOW_INTEGRATION_PHASE=full` in `/home/nikhil/Alpha-net/backend/.env`; `getFlowIntegrationPhase() !== 'legacy'` is the invoice default so **every** stage from `clients` upward uses approved-only, with unset/`legacy` restoring the original behavior and per-request `approvedOnly:false` as the no-redeploy escape hatch. Tests: `flow-phase9.test.ts` 9.1 (3/3 — full cutover bills only approved/unbilled rows and writes `lines`+`billedTimesheetIds`; explicit `false` and `legacy` both revert to sum-all). `invoice-lines.test.ts` retains the 4 flag permutations. Operational check: no request/error telemetry exists in this repo, so "error rate" was verified as **zero failing tests + zero tsc errors + zero backfill errors**, not as a production traffic metric — production monitoring remains a deployment-time operational task.
- [x] 9.2 `assignmentId` required on NEW timesheets only after 100% backfill: `createTimesheet()` rejects only when `isFlowPhaseEnabled('full')` AND no active assignment resolves (error `assignmentId is required: no active assignment covers this resource, project, and week`); earlier stages keep Phase 4's leniency and existing documents are never rewritten. Gate precondition proven live: `backfill-timesheet-assignments --dry-run` → `timesheetsScanned=30 missingLink=0 linked=0 skippedNoAssignment=0 errors=0` (30/30 linked), `backfill-assignments --dry-run` → `pairsConsidered=10 created=0 existing=10`. Tests: 9.2 (3/3 — unlinked new row rejected with zero documents written, active assignment auto-attaches, legacy unlinked row still reads).
- [x] 9.3 Legacy fields retained (no deprecation < 2 releases): retention policy documented in `lib/collections.ts` (Phase 9 header) and `.env.example`; `projects.client/sowNumber/hourlyRate/teamMemberIds`, timesheet `userId+projectId` scoping, legacy document rows and legacy invoice totals all keep working under `full`. Tests: 9.3 (2/2), plus `flow-baseline.test.ts` (13/13) and the Phase 4/5/6 legacy-path suites.
- [x] 9.4 Edge tests (duplicate backfills, concurrent invoices, large teams): `flow-phase9.test.ts` 9.4 (4/4) — assignment backfill run twice → still exactly 1 active assignment; invoice-line backfill run twice → invoice count stays 1 and `total` stays 880 (totals-preserving); two concurrent `createInvoice()` calls (`Promise.allSettled`) → 1 fulfilled / 1 rejected and exactly one draft holding exactly one `billedTimesheetIds` entry; 250-member project backfill → `teamMemberIds=250` and 250 active assignments with no truncation. Live dry-runs: clients `clientsCreated=0 clientsExisting=2 projectsUpdated=0`; assignments `created=0 existing=10`; timesheet links `missingLink=0`; invoice lines `pending=0 maxTotalsDiff=0` (all `errors=0`).
- [x] 9.5 Final `npm run build && npm test` green: `npx tsc --noEmit` **0 errors** (was 52/44 pre-existing test-fixture diagnostics; fixed by typing the existing mocks only — no production behavior change), `npm run build` **exit 0**, `npm test` **38 files / 411 tests passed** (was 37/397; the historical `invoice-pdf` "INVOICE" assertion now passes via correct PDF text decoding), `flow-phase9.test.ts` **14/14**. `npm run lint` still reports 102 pre-existing lint errors across the repo (unused vars / `@ts-ignore` / triple-slash refs) — these are outside Phase 9's scope and are not part of the build.
- [x] 9.6 Docx demo `Resource→Assignment→Timesheet(approved,locked)→Invoice(lines)→Payroll→Margin` (Sony `ASG-00125/TS-1001/$18,480/$10,920/$7,560`): `flow-phase9.test.ts` 9.6 (2/2). End-to-end test traces `resourceId → assignmentId → timesheetId` through `collectBillableTimesheets → createPayrollFromTimesheet → markPayrollPaid → getMargin`; the Sony demo uses four approved+locked weekly timesheets (168 total hours), `payRate=65` → payroll $10,920 and `grossMargin=7560`. **Known basis difference from the docx:** the platform bills regular Mon–Fri hours only (max 40h/timesheet), so the docx's single-timesheet $18,480 would need a 168-billable-hour week; at four 40h weeks the invoice is $17,600 while the $110/$65 margin math is identical. The docx figures themselves are asserted exactly in `margin.test.ts` (168h × $110 = $18,480, − $10,920 = $7,560 @ 40.9%) and by arithmetic in 9.6.

## Progress Summary (update weekly)

| Phase | Status | Evidence | Owner | Date |
|---|---|---|---|---|
| 0 Baseline | `[x]` | Phase 0 done 2026-09-24: flow-baseline 13/13; suite 258/259 (1 pre-existing invoice-pdf fail, fails on stashed main too); tsc 52 pre-existing, 0 new | Cline | 2026-09-24 |
| 1 Clients | `[x]` | Done 2026-09-24: 10/10 clients tests; live backfill 2 clients + 3 projects linked, 2nd run clean; suite 268/269 (1 pre-existing); tsc diff empty | Cline | 2026-09-24 |
| 2 Resources | `[x]` | Done 2026-09-24: 9/9 resource tests; user regressions 18/18; suite 277/278 (1 pre-existing); tsc diff empty | Cline | 2026-09-24 |
| 3 Assignments | `[x]` | Done 2026-09-24: 25/25 assignment tests; live backfill 10 assignments from 3 projects (2nd run created 0/existing 10, 0 dup pairs, all with clientId); suite 302/303 (1 pre-existing invoice-pdf); tsc diff empty | Cline | 2026-09-24 |
| 4 Timesheets | `[x]` | Done 2026-09-24: 30/30 phase-4 tests; live backfill 30 timesheets linked (2nd run `missingLink=0`, `mismatchedPairs=0`, `nonActiveTargets=0`); `timesheets` index `assignmentId_1` added, unique `(userId,projectId,weekStart)` intact; suite 332/333 (1 pre-existing invoice-pdf); tsc diff empty | Cline | 2026-09-24 |
| 5 Invoices | `[x]` | Done 2026-09-24: 22/22 invoice-lines tests; live backfill 2 invoices lines reconstructed, run2 `pending=0`, per-invoice `diff=0` (`INV-2026-0001 240=240`, `INV-2026-0002 81593.2=81593.2`); `invoices` indexes `billedTimesheetIds_1`+`assignmentId_1` live; cutover flag `FLOW_INTEGRATION_PHASE=invoices` flipped (payroll still gated); suite 354/355 (1 pre-existing invoice-pdf); tsc 52 = baseline, 0 new | Cline | 2026-09-24 |
| 6 Payroll | `[x]` | Done 2026-09-24: 10/10 phase-6 tests (pure preview 168h×$65=$10,920, rate-chain payRateMissing guard, approved-only gate, idempotent create via unique timesheetId, W2/C2C/unknown branch, pay/void lifecycle, list filters, 401 mount); `ensurePayrollIndexes()` from server.ts (baseline `ensureIndexes` still never touches `payrolls`); invoicing untouched (35/35 invoice-lines+baseline, mtimes prove no invoice edit); suite 364/365 (1 pre-existing invoice-pdf); tsc 52 = baseline, 0 new | Cline | 2026-09-24 |
| 7 Margin | `[x]` | Done 2026-09-24: 10/10 margin tests (docx §6 fixture 168h×$110=$18,480 − 168h×$65=$10,920 = $7,560 @ 40.9%; assignment/project/period filters; approved default + `status:'all'` opt-out; shared `resolvePayRate` chain with payroll; void/billRate:0/unbilled honesty cases; route 200/403/401 + match-plan asserts); read-only view — no app.ts/collections/server edits (reports router already mounted); suite 374/375 (1 pre-existing invoice-pdf); tsc 52 = baseline, 0 new | Cline | 2026-09-24 |
| 8 Polish | `[x]` | Done 2026-09-24: 8.1 documents metadata + onboarding filters (8/8); 8.2 PO/SOW computed balance (5/5); 8.3 CRON_SECRET rollover endpoint (6/6); 8.4 status map + `isLocked` (3/3); flow-phase8 suite **22/22**; full suite 396/397 at the time (the single PDF-text assertion is now fixed in 9.5) | Cline | 2026-09-24 |
| 9 Cutover | `[x]` | Done 2026-09-26: `FLOW_INTEGRATION_PHASE=full`; NEW timesheets must resolve an assignment; legacy paths retained (2-release minimum); edge tests (duplicate backfills, concurrent invoice creates, 250-member team) 4/4; live backfills all clean (`missingLink=0`, `created=0 existing=10`, `pending=0 maxTotalsDiff=0`); tsc **0 errors**, build exit 0, tests **411/411 across 38 files**, flow-phase9 **14/14** | Cline | 2026-09-26 |

## Risk Log

| Date | Risk | Mitigation | Status |
|---|---|---|---|
| 2026-09-24 | Phase 5 over-billing on cutover | flag + totals-preserving backfill + `alreadyBilled` guard | `[x]` 2026-09-24 (flag default legacy until flip; backfill run1 `updated=2 maxTotalsDiff=0`, run2 `pending=0`, live verify `diff=0` both invoices; guard tests: reserved excluded, all-reserved throws, void releases, paid keeps) |
| 2026-09-24 | Approved-only cutover bricks invoicing if wrong (empty bills / stuck drafts) | `approvedOnly` explicit per-request `false` reverts without redeploy; flag off = legacy path byte-identical; one-draft guard runs before collection with actionable message | `[x]` 2026-09-24 (4 flag-permutation tests; flag flipped to `invoices`, suite green after flip) |
| 2026-09-24 | Backfilled `billRate:0` silently produces $0 invoice lines | rate snapshot treats `0/null/undefined` as unset → falls back to invoice `hourlyRate`, records `rateSource:'invoice'` | `[x]` 2026-09-24 (test "treats a backfilled billRate of 0 as unset — never a silent $0 line") |
| 2026-09-24 | `paid`/`void` reachable by stale UI → edits to settled money | `assertDraftInvoice` status-specific blocks on all 3 draft mutations; `send`/`pay`/`void` each reject terminal states; paid is terminal (no void), void not twice-allowed | `[x]` 2026-09-24 (paid-terminality 5/5 + void lifecycle tests) |
| 2026-09-24 | `payRate=0` silent math | `payRateMissing:true`, block pay until fixed | `[x]` 2026-09-24 (rate chain treats 0/null/undefined as unset at BOTH sources — assignment.payRate→user.defaultPayRate→missing; `markPayrollPaid` throws actionable message while missing; test proves block → fix assignment rate → re-create refreshes same doc with gross $10,920 → pay succeeds) |
| 2026-09-24 | Margin view overstates when rates are missing (unresolved pay → $0 cost) or before billing (pct denominator 0) | view stores no money — read-only; actual payment stays blocked by Phase 6's `payRateMissing` gate; `billRate:0` reports NEGATIVE margin (visible, never silent); `marginPct` guarded to 0 while unbilled | `[x]` 2026-09-24 (tests: rate-chain missing → `payrollCost:0` documented with the gate caveat; `billRate:0` → −$10,920; unbilled & void-invoice worlds → pct 0, never NaN/Infinity) |
| | Locked/approved timesheets silently edited (money drift) | `isLocked` guard FIRST in `update/submit/withdraw` + `isLocked/lockedAt` written in the same `$set` as approval; corrections go through a new `adjustmentOf` timesheet | `[x]` 2026-09-24 (Phase 4: 4 lock tests, atomic-approve test) |
| | Backfill dupes (`Sony`/`sony`, pair dupes) | normalized upsert + `findOne` guards + 2nd-run `created 0` | `[x]` clients 2026-09-24 (2 clients, 2nd run clean) · `[x]` assignments 2026-09-24 (10 created, 2nd run `created=0 existing=10`, 0 dup active pairs) · `[x]` timesheets 2026-09-24 (30 linked, 2nd run `missingLink=0`, 0 mismatched pairs) |


| 2026-09-26 | Backfilled legacy assignments keep `billRate:0` / `payRate:0` until real rates are entered (decision: do not invent rates) | margin view reports the gap honestly (billRate 0 → negative margin; missing pay → $0 cost but `payRateMissing:true` blocks actual payment); invoice lines fall back to the invoice rate; payroll pay blocked | `[x]` 2026-09-26 (operator decision; enforced by payroll gate + margin tests) |
| 2026-09-26 | Cutover default now applies at every stage above `legacy`, not only `invoices`+ | `FLOW_INTEGRATION_PHASE=legacy` (or unset) restores sum-all; per-request `approvedOnly:false` is the no-redeploy escape hatch | `[x]` 2026-09-26 (flow-phase9 9.1 3/3; invoice-lines 4 flag permutations) |
| 2026-09-26 | The docx's 168-hour billing week exceeds the platform's billable rule (regular Mon–Fri hours, ≤40h per timesheet) | payroll/margin use all worked hours (`totalHours`) as the docx does; invoicing keeps the legacy weekday rule so no weekend/overtime revenue changes silently — align billing rules explicitly before billing overtime | `[x]` 2026-09-26 (documented in checklist 9.6; demo shows 168h → $17,600 at 4×40h, exact docx math asserted in `margin.test.ts`) |
| 2026-09-26 | `npm run lint` still fails (102 pre-existing errors: unused vars, `@ts-ignore`, triple-slash refs) | build (`tsc`) is green and lint is not part of the build/test gate; tracked as a separate cleanup task | `[ ]` separate cleanup task |